export enum NODE_ENV {
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
}

//TODO: move it to environment variable, or configuration service
export const jwtConstants = {
    secret: 'secretKey',
};
